<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f0f0f0;
}

form {
    width: 300px;
    margin: 50px auto;
    padding: 20px;
    background-color: #fff;
    border-radius: 5px;
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
}

input[type="checkbox"] {
    margin-right: 5px;
}

input[type="text"],
input[type="date"] {
    width: calc(100% - 20px);
    padding: 10px;
    margin-bottom: 10px;
    border: 1px solid #ccc;
    border-radius: 5px;
}

input[type="submit"] {
    width: 100%;
    padding: 10px;
    background-color: #007bff;
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

input[type="submit"]:hover {
    background-color: #0056b3;
}




</style>
<body>
    <form action="/book" method="post">
        <% for (var i=0; i < users.length; i++){ %>
            <input type="checkbox" name="users" value="<%= users[i]._id %>">
            <%= users[i].name %><br>
        <% } %>
        
        <input type="text" name="book" id="book" placeholder="book name">
        <input type="text" name="author" id="author" placeholder="author name">
        <input type="tel" name="published" id="published" placeholder="published year">
        <input type="submit" value="submit">
    </form>
    
</body>
</html>

  